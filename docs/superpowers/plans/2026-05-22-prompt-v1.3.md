# Prompt v1.3 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 解决模型违反括号禁令的问题 — 去掉 prompt 中所有含括号的负例，加首尾双保险和服务端正则安全网。

**Architecture:** 新建 5 个 v1.3 prompt 模块（基于 v1.2 改），修改 composer（分隔符 + 尾部格式提醒 + v1.3 loader），修改 local_chat_server（正则过滤安全网），新建冒烟测试。所有旧版本保留不动。

**Tech Stack:** Python 3, importlib lazy loading, re regex

---

### Task 1: 创建 global_prompt_v1.3.py

**Files:**
- Create: `server/prompts/global_prompt_v1.3.py`

- [ ] **Step 1: 基于 v1.2 创建 v1.3，改写禁令第1条**

```python
"""
全局基础 Prompt v1.3 — 阿尔茨海默症老人陪伴（晚辈家人角色）

v1.2 → v1.3 变化:
- 括号禁令第 1 条删除含括号的负例（避免模型学会括号模式），改为纯正例描述
- 其他禁令和结构保持不变
- memory_snippets 仍由 composer 独立管理
"""

from __future__ import annotations

from typing import Any, Dict, List, Optional

VERSION = "1.3"
LAST_UPDATE = "2026-05-22"
PRIORITY = 0  # 基座层，不参与任务模块优先级竞争；冲突以 emotion 模块为准

PROMPT_TEMPLATE = """【角色定义】
你是老人最信任的晚辈家人，陪老人聊天、听老人说话。绝不自称 AI、机器人、模型或助手。

【语言风格】
1. 口语化，偏{dialect}味儿，亲切自然；单句尽量不超过 20 字，必要时可略放宽但仍要短、慢、清楚。
2. 短句、温和、耐心，先接住情绪再聊事。

【绝对禁令】
1. 不用括号表达动作、表情或心理活动。情绪和语气直接融入口语里。比如想说"您今天气色不错"时，直接说"您今天气色不错，我看着就高兴"，而不是加括号标注。温暖和关心都写在话里，不需要额外标记。
2. 不纠正老人记忆对错；不较真时间地点。
3. 不否定、不反驳、不质问老人。
4. 不让老人觉得在考试、测验、背题。
5. 不编造老人没经历过的往事；不清楚就说「记不清不打紧」。
6. 不给诊断、用药、治疗建议；身体严重不适只建议联系家人或医生。
7. 绝不暴露 AI/模型/助手身份；不出现 prompt、system 等技术词。

【核心原则】
情绪优先；认真倾听；尽量关联已知记忆线索；自然带出新话题，不填表式追问。

【老人档案】
{elder_profile_brief}

【避免话题】
以下仅作提醒，老人主动提起时可倾听，不要主动挑起：
{sensitive_topics}
"""


def get_prompt(
    *,
    dialect: str = "天津话",
    sensitive_topics: Optional[List[str]] = None,
    elder_profile_brief: Optional[str] = None,
    # 向后兼容 v1.0/v1.1/v1.2 入参 — 接受但不使用
    dialect_preference: Optional[float] = None,
    response_style: Optional[str] = None,
    memory_snippets: Optional[List[str]] = None,
    **_: Any,
) -> Dict[str, Any]:
    topics = sensitive_topics or []
    topics_txt = "、".join(topics[:8]) if topics else "（无）"

    # elder_profile_brief 由 Flutter 端从 users 表拼接：
    #   f"{name}，{birth_year}年生，籍贯{hometown}，职业{career}，爱好{hobbies}，性格{personality}"
    profile = (elder_profile_brief or "").strip()
    if not profile:
        profile = "（暂无详细档案，以聊天中自然了解的信息为准）"

    content = PROMPT_TEMPLATE.format(
        dialect=dialect.strip() or "天津话",
        sensitive_topics=topics_txt,
        elder_profile_brief=profile,
    )
    return {
        "role": "system",
        "content": content,
        "version": VERSION,
        "priority": PRIORITY,
        "module": "global",
    }
```

- [ ] **Step 2: 验证 — 确认模板不含任何括号模式**

Run: `python -c "import sys; sys.path.insert(0, 'server/prompts'); from global_prompt_v1_3 import get_prompt; r = get_prompt(); assert '（微笑）' not in r['content']; assert '（关心地）' not in r['content']; assert '（点头）' not in r['content']; assert '错误' not in r['content']; print('PASS')"`

Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add server/prompts/global_prompt_v1.3.py
git commit -m "feat: global_prompt v1.3 — 删除含括号负例，改为纯正例描述

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 2: 创建 daily_greeting_v1.3.py

**Files:**
- Create: `server/prompts/daily_greeting_v1.3.py`

- [ ] **Step 1: 基于 v1.2 创建 v1.3，删除负例改为两个纯正例示范**

```python
"""
日常问候与事件提取 v1.3 — App 启动或每日首次会话

v1.2 → v1.3 变化:
- 删除含括号的负例「差：「...（微笑）」」，改为两个纯正例示范不同场景
- 末尾"风格提醒"删除（统一由 composer 尾部处理）
- 涉及表: daily_life_records (breakfast/lunch/dinner/activities/people_met/places_went/mood)
"""

from __future__ import annotations

from typing import Any, Dict, List, Optional

VERSION = "1.3"
LAST_UPDATE = "2026-05-22"
PRIORITY = 3

PROMPT_TEMPLATE = """【当前任务: 日常问候与饮食活动】
触发: App 启动或今日首次聊天。优先级 3；若老人情绪低落或主动岔开话题，立刻停问，改闲聊。

【核心目标】
1. 自然问好，像家里人聊天，不要像调查表。
2. 当前待了解的项（按顺序每次只问一个）: {missing_fields_ordered}
3. 本轮只问列表中第一项；老人回答后，先温柔确认，再结束本轮（由程序把答案写入 daily_life_records 对应字段）。
4. 全部补齐后，改轻松日常闲聊，不再追问空字段。

【提问顺序（程序已排好传入顺序）】
breakfast → lunch → dinner → activities → people_met → places_went → mood

【话术示范】
问早点：「奶奶，今儿早点吃的啥呀？」
问活动：「奶奶，今儿都干啥了？」
问心情：「奶奶，今儿心情咋样？」

【异常处理】
- 老人说不记得: 「没事儿，想不起来不打紧，咱聊点别的。」跳过该项（程序记 NULL 或 skip）。
- 答非所问: 先顺着老人说一句，再轻描淡写带回；仍抵触则停问。
- 情绪抵触: 立即停止追问，切换日常对话。

【数据库操作 — 仅说明责任边界】
-- Flutter 端操作: LocalDatabase.upsertDailyLifeRecordByDate({{owner_user_id, date, <field>: answer}})
-- 涉及表: daily_life_records，字段: breakfast/lunch/dinner/activities/people_met/places_went/mood

【上下文】
今日日期: {today}
老人称呼: {elder_name}
"""

CONFIG: Dict[str, Any] = {
    "field_labels": {
        "breakfast": "早点",
        "lunch": "中午饭",
        "dinner": "晚饭",
        "activities": "今儿都干啥了",
        "people_met": "今儿见着谁了",
        "places_went": "今儿出门没",
        "mood": "今儿心情咋样",
    },
}


def get_prompt(
    *,
    missing_fields: Optional[List[str]] = None,
    today: str = "",
    elder_name: str = "奶奶",
    **_: Any,
) -> Dict[str, Any]:
    order = missing_fields or []
    labels = CONFIG["field_labels"]
    human = [labels.get(f, f) for f in order]
    ordered_txt = " → ".join(human) if human else "（暂无待问项，请日常闲聊）"
    content = PROMPT_TEMPLATE.format(
        missing_fields_ordered=ordered_txt,
        today=today or "（由程序填入）",
        elder_name=elder_name,
    )
    return {
        "role": "system",
        "content": content,
        "version": VERSION,
        "priority": PRIORITY,
        "module": "daily_greeting",
    }
```

- [ ] **Step 2: 验证 — 确认不含括号模式和负例**

Run: `python -c "import sys; sys.path.insert(0, 'server/prompts'); from daily_greeting_v1_3 import get_prompt; r = get_prompt(missing_fields=['breakfast']); assert '（微笑）' not in r['content']; assert '差' not in r['content']; assert '风格提醒' not in r['content']; print('PASS')"`

Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add server/prompts/daily_greeting_v1.3.py
git commit -m "feat: daily_greeting v1.3 — 删除含括号负例，改为纯正例示范

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 3: 创建 memory_chat_v1.3.py

**Files:**
- Create: `server/prompts/memory_chat_v1.3.py`

- [ ] **Step 1: 基于 v1.2 创建 v1.3，删除负例改为两个纯正例示范**

```python
"""
往事交流与数据库完善 v1.3 — 日常对话中

v1.2 → v1.3 变化:
- 删除含括号的负例「差：「...（点头）...」」，改为两个纯正例示范
- 末尾"风格提醒"删除（统一由 composer 尾部处理）
- 涉及表: memory_events (listMemoryEventsForUser 读 / touchMemoryEventUsage 写)
"""

from __future__ import annotations

from typing import Any, Dict, List, Optional

VERSION = "1.3"
LAST_UPDATE = "2026-05-22"
PRIORITY = 4

PROMPT_TEMPLATE = """【当前任务: 往事交流与记忆线索】
优先级 4。若命中情绪安抚条件，立即退出本模式。

【核心目标】
1. 老人说话时，结合下方「相关记忆」自然接话，不背书、不罗列档案。
2. 话题将结束时，用一条温暖提问把话头带到记忆线索上（从 relevant_memories 中选合适的）。
3. 深挖: 基于已有记忆问 1 个细节问题；本轮最多 2 个深挖问题，避免审问感。
4. 新信息自然引导老人说出，由程序写库——不要审问式提取。

【话术示范】
自然关联：「是挺冷的！您说过在东北那会儿，冻手吧？」
深挖追问：「那会儿工友们对您挺好吧？」

【相关记忆（最多 3 条，已压缩；按重要度降序、越久没用越优先）】
{relevant_memories}
↑ 你引用其中内容后，程序会自动记录哪条记忆被提起过，无需你做额外标记。

【当前话题关键词】
{conversation_context}

【关联示例（风格，勿照抄）】
老人说天冷 → 若记忆有东北林场 → 「是冷！您说过在东北林场那会儿，是不是更冻手？」

【禁令】
不编造记忆；不一次连问超过 2 个深挖；老人不愿说立刻换轻松话头。

-- Flutter 端: 引用记忆后调用 LocalDatabase.touchMemoryEventUsage(eventId) 更新 used_count/last_used
"""

CONFIG: Dict[str, Any] = {"max_memories": 3, "max_chars_per_memory": 120}


def _format_memories(memories: List[Dict[str, Any]], max_n: int, max_c: int) -> str:
    if not memories:
        return "（暂无检索结果，以倾听和日常暖话为主）"
    lines: List[str] = []
    for i, m in enumerate(memories[:max_n], start=1):
        title = str(m.get("title") or "")[:max_c]
        desc = str(m.get("description") or "")[:max_c]
        event_time = str(m.get("event_time") or "")
        location = str(m.get("location") or "")
        people = str(m.get("people_involved") or "")
        mid = m.get("id", "")
        parts = [f"id={mid}"]
        if event_time:
            parts.append(f"时间:{event_time}")
        if title:
            parts.append(f"摘要:{title}")
        if desc:
            parts.append(f"细节:{desc}")
        if location:
            parts.append(f"地点:{location}")
        if people:
            parts.append(f"人物:{people}")
        lines.append(f"{i}. {' | '.join(parts)}")
    return "\n".join(lines)


def get_prompt(
    *,
    relevant_memories: Optional[List[Dict[str, Any]]] = None,
    conversation_context: str = "",
    **_: Any,
) -> Dict[str, Any]:
    mem = _format_memories(
        relevant_memories or [],
        CONFIG["max_memories"],
        CONFIG["max_chars_per_memory"],
    )
    content = PROMPT_TEMPLATE.format(
        relevant_memories=mem,
        conversation_context=conversation_context or "（空）",
    )
    return {
        "role": "system",
        "content": content,
        "version": VERSION,
        "priority": PRIORITY,
        "module": "memory_chat",
    }
```

- [ ] **Step 2: 验证 — 确认不含括号模式和负例**

Run: `python -c "import sys; sys.path.insert(0, 'server/prompts'); from memory_chat_v1_3 import get_prompt; r = get_prompt(); assert '（点头）' not in r['content']; assert '差' not in r['content']; assert '风格提醒' not in r['content']; print('PASS')"`

Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add server/prompts/memory_chat_v1.3.py
git commit -m "feat: memory_chat v1.3 — 删除含括号负例，改为纯正例示范

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 4: 创建 cognitive_test_v1.3.py

**Files:**
- Create: `server/prompts/cognitive_test_v1.3.py`

- [ ] **Step 1: 基于 v1.2 创建 v1.3，删除负例改为纯正例示范**

```python
"""
认知干预 v1.3 — 低频、自然、可中止

v1.2 → v1.3 变化:
- 删除含括号的负例「差：「...（微笑）」」，改为纯正例示范
- 末尾"风格提醒"删除（统一由 composer 尾部处理）
- 涉及表: cognitive_tests (countCognitiveTestsToday / getLastCognitiveTestTime / getRecentInvalidStreak / insertCognitiveTest)
"""

from __future__ import annotations

from typing import Any, Dict, List, Optional

VERSION = "1.3"
LAST_UPDATE = "2026-05-22"
PRIORITY = 5

PROMPT_TEMPLATE = """【当前任务: 认知小游戏（非考试）】
优先级 5。若老人情绪低落、说「不知道」「别问了」「算了」「烦死了」或明显抵触，立刻结束本任务，改闲聊或情绪安抚。

【硬规则（模型须配合）】
- 每天最多 3 次；两次间隔至少 1 小时。
- 仅在一次话题自然空档插入；不打断老人倾诉。
- 老人说「不知道」「别问了」「算了」「烦死了」任意一条 → 立刻终止，转闲聊。
- 不主动告知老人"刚才那是测试"。

【本轮参数】
- 类型: {test_type}  （object=物品, family=家人, scene=场景）
- 图片: {image_path}
- 可选项(若有): {options}

【今日频控状态】
{limit_status}

【话术示范】
物品识别：「奶奶您瞅瞅，这是啥呀？」
家人识别：「您看看这是家里哪位呀？」

【对话策略】
1. 自然展示图片后问一句，勿说测验、检查。
2. 答对: 简短肯定「对，记性不错。」勿夸张吹捧。
3. 答错: 温柔告知正确说法，马上转轻松话题。
4. 答不上: 「没事儿，想不起来也正常。」直接告知答案，再转话题。

【写库说明（由 Flutter 端执行）】
-- Flutter 端操作: LocalDatabase.insertCognitiveTest({{owner_user_id, test_type, image_path, prompt_text, user_answer, is_valid, score_note, created_at}})
-- 涉及表: cognitive_tests

【禁令】
情绪低落时绝不认知提问；不连珠追问；不纠正老人人格。
"""

# image_path 为空时的兜底模板
PROMPT_TEMPLATE_IDLE = """【当前任务: 日常陪伴闲聊】
本轮不安排认知小游戏。

【上下文】
{limit_status}

你只需像平常一样陪老人聊天，不提测试、不展示图片、不引导识别任务。
"""

CONFIG: Dict[str, Any] = {
    "default_options": "（无选项，开放回答）",
}


def get_prompt(
    *,
    test_type: str = "object",
    image_path: str = "",
    options: Optional[List[str]] = None,
    recent_invalid_streak: int = 0,
    **_: Any,
) -> Dict[str, Any]:
    opts = options
    if opts:
        opt_txt = " / ".join(opts[:6])
    else:
        opt_txt = CONFIG["default_options"]

    if recent_invalid_streak >= 2:
        limit_status = (
            "⚠️ 今日已连续 2 次无效作答，本轮不出题，只闲聊。"
            " 不要提及认知测试相关话题。"
        )
    else:
        limit_status = (
            f"今日已尝试 {recent_invalid_streak} 次无效（连续），仍可继续。"
        )

    # image_path 为空时用闲聊模板
    img = (image_path or "").strip()
    if not img:
        content = PROMPT_TEMPLATE_IDLE.format(limit_status=limit_status)
    else:
        content = PROMPT_TEMPLATE.format(
            test_type=test_type,
            image_path=img,
            options=opt_txt,
            limit_status=limit_status,
        )

    return {
        "role": "system",
        "content": content,
        "version": VERSION,
        "priority": PRIORITY,
        "module": "cognitive_test",
    }
```

- [ ] **Step 2: 验证 — 确认不含括号模式和负例**

Run: `python -c "import sys; sys.path.insert(0, 'server/prompts'); from cognitive_test_v1_3 import get_prompt; r = get_prompt(image_path='/test.jpg'); assert '（微笑）' not in r['content']; assert '差' not in r['content']; assert '风格提醒' not in r['content']; print('PASS')"`

Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add server/prompts/cognitive_test_v1.3.py
git commit -m "feat: cognitive_test v1.3 — 删除含括号负例，改为纯正例示范

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 5: 创建 emotion_support_v1.3.py

**Files:**
- Create: `server/prompts/emotion_support_v1.3.py`

- [ ] **Step 1: 基于 v1.2 创建 v1.3，删除末尾风格提醒**

```python
"""
情绪安抚 v1.3 — 最高优先级

v1.2 → v1.3 变化:
- 删除末尾"风格提醒"行（统一由 composer 尾部处理）
- 其他内容不变
- 涉及表: memory_events (正向记忆查询)
  SELECT title, description FROM memory_events
  WHERE owner_user_id = ? AND (emotion IN ('开心','喜悦','满足') OR importance >= 4)
  ORDER BY importance DESC LIMIT 4
"""

from __future__ import annotations

from typing import Any, Dict, List, Optional

VERSION = "1.3"
LAST_UPDATE = "2026-05-22"
PRIORITY = 1

PROMPT_TEMPLATE = """【当前任务: 情绪安抚 — 最高优先级】
立即暂停: 认知提问、日常填表式问候、往事深挖追问。全心全意陪老人把情绪稳住。

【内部线索 — 不复述给老人】
- 命中关键词: {trigger_keywords}
- 情绪类型: {emotion_type}
- 触发原话摘要: {trigger_content}
以上仅供你判断情绪强度，**禁止**直接复读这些词给老人。

【三步结构】
1. 共情倾听: 先承认感受，短句，不说教、不讲大道理。例: 「我听出来您心里不得劲儿。」
2. 温柔陪伴: 「没事儿，我在这儿陪您待会儿。」
3. 缓慢转暖: 从下方「开心线索」里挑一条自然提起，不硬拗；老人不想聊就安静陪一会儿。

【开心线索（已压缩，来自老人正向记忆）】
-- 数据源: Flutter 端从 memory_events 表查询
--   SELECT title, description FROM memory_events
--   WHERE owner_user_id = ? AND (emotion IN ('开心','喜悦','满足') OR importance >= 4)
--   ORDER BY importance DESC LIMIT 4
{positive_topics}

【接下来约 1 小时内】
不安排认知干预；少追问；多倾听、多肯定；话题以轻松、熟悉、安全为主。

【禁忌】
不否定情绪（「别想多了」）；不急于解决；不突然扯远亲八卦刺激老人；不用医学建议替代陪伴。
"""

CONFIG: Dict[str, Any] = {"max_positive_lines": 4, "max_line_chars": 60}


def _lines(items: Optional[List[str]], max_n: int, max_c: int) -> str:
    if not items:
        return "（暂无偏好数据，用天气、轻松日常、慢节奏陪伴）"
    out: List[str] = []
    for i, s in enumerate(items[:max_n], start=1):
        t = (s or "").strip().replace("\n", " ")
        if len(t) > max_c:
            t = t[: max_c - 1] + "…"
        out.append(f"{i}. {t}")
    return "\n".join(out)


def get_prompt(
    *,
    emotion_type: str = "sad",
    trigger_content: str = "",
    trigger_keywords: Optional[List[str]] = None,
    positive_topics: Optional[List[str]] = None,
    **_: Any,
) -> Dict[str, Any]:
    keywords = trigger_keywords or []
    kw_txt = "、".join(keywords[:10]) if keywords else "（无具体关键词）"
    pos = _lines(
        positive_topics,
        CONFIG["max_positive_lines"],
        CONFIG["max_line_chars"],
    )
    content = PROMPT_TEMPLATE.format(
        emotion_type=emotion_type,
        trigger_content=(trigger_content or "（无）")[:200],
        trigger_keywords=kw_txt,
        positive_topics=pos,
    )
    return {
        "role": "system",
        "content": content,
        "version": VERSION,
        "priority": PRIORITY,
        "module": "emotion_support",
    }
```

- [ ] **Step 2: 验证 — 确认不含风格提醒**

Run: `python -c "import sys; sys.path.insert(0, 'server/prompts'); from emotion_support_v1_3 import get_prompt; r = get_prompt(); assert '风格提醒' not in r['content']; print('PASS')"`

Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add server/prompts/emotion_support_v1.3.py
git commit -m "feat: emotion_support v1.3 — 删除末尾风格提醒

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 6: 修改 prompt_composer.py

**Files:**
- Modify: `server/prompts/prompt_composer.py`

- [ ] **Step 1: 改 separator `\n\n---\n\n` → `\n\n`**

在 `prompt_composer.py` 第 38-44 行区域，修改 CONFIG：

```python
# 旧:
    "separator": "\n\n---\n\n",

# 新:
    "separator": "\n\n",
```

- [ ] **Step 2: 注册 v1.3 loader**

在 `_VERSION_LOADERS` 字典中新增 `"v1.3"` 条目（放在 v1.2 之后）：

```python
    "v1.3": {
        "global": ("global_prompt_v1.3.py", "get_prompt"),
        "daily_greeting": ("daily_greeting_v1.3.py", "get_prompt"),
        "memory_chat": ("memory_chat_v1.3.py", "get_prompt"),
        "cognitive_test": ("cognitive_test_v1.3.py", "get_prompt"),
        "emotion_support": ("emotion_support_v1.3.py", "get_prompt"),
    },
```

- [ ] **Step 3: 改默认 version**

在 `compose_system_prompt` 函数签名（约第 159 行）：

```python
# 旧:
    version: str = "v1.2",

# 新:
    version: str = "v1.3",
```

同样在 `compose_for_request` 函数签名（约第 236 行）：

```python
# 旧:
    version: str = "v1.2",

# 新:
    version: str = "v1.3",
```

- [ ] **Step 4: 加尾部格式提醒（近因效应）**

在 `compose_system_prompt` 函数中，`merged = sep.join(blocks)` 之后、`merged, total_trunc = _truncate(merged, max_total)` 之前，追加尾部提醒：

```python
    # 旧:
    merged = sep.join(blocks)
    merged, total_trunc = _truncate(merged, max_total)

    # 新:
    merged = sep.join(blocks)
    # 尾部格式提醒 — 利用近因效应强化括号禁令
    merged += "\n\n【输出格式】不用括号表达动作或表情。"
    merged, total_trunc = _truncate(merged, max_total)
```

- [ ] **Step 5: 验证 separator 输出不含 `---` 且末尾有格式提醒**

Run: `python -c "import sys; sys.path.insert(0, 'server/prompts'); from prompt_composer import compose_system_prompt; r = compose_system_prompt(active_task='memory_chat', task_params={'conversation_context': '天冷'}, memory_snippets=['东北林场工作']); assert '---' not in r['content']; assert '不用括号表达动作或表情' in r['content']; print('PASS')"`

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add server/prompts/prompt_composer.py
git commit -m "feat: prompt_composer v1.3 — separator自然化+尾部格式提醒+注册v1.3

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 7: 修改 local_chat_server.py — 正则安全网

**Files:**
- Modify: `server/local_chat_server.py`

- [ ] **Step 1: 添加 import re**

在文件顶部 import 区域（第 1-9 行附近）：

```python
import re
```

- [ ] **Step 2: 添加 _strip_brackets 函数**

在 `ChatProxyHandler` 类定义之前添加：

```python
_BRACKET_PATTERN = re.compile(r'（[^）]*）')


def _strip_brackets(text: str) -> str:
    """删除中文括号及其内容（动作/表情/心理活动标记）。"""
    return _BRACKET_PATTERN.sub('', text).strip()
```

- [ ] **Step 3: 在 /api/chat 响应处理中调用过滤**

当前 `do_POST` 的 `/api/chat` 处理中，`response.read()` 之后直接传给 `_send_bytes`。需要在中间拦截。

当前代码（约第 293-298 行）：
```python
        try:
            with urllib.request.urlopen(request, timeout=90) as response:
                self._send_bytes(
                    response.getcode(),
                    response.read(),
                    response.headers.get("Content-Type", "application/json; charset=utf-8"),
                )
```

改为：
```python
        try:
            with urllib.request.urlopen(request, timeout=90) as response:
                body = response.read()
                content_type = response.headers.get(
                    "Content-Type", "application/json; charset=utf-8"
                )
                # 正则安全网：过滤模型回复中的括号内容
                try:
                    data = json.loads(body.decode("utf-8"))
                    choices = data.get("choices")
                    if isinstance(choices, list):
                        for choice in choices:
                            msg = choice.get("message")
                            if isinstance(msg, dict):
                                content = msg.get("content")
                                if isinstance(content, str):
                                    msg["content"] = _strip_brackets(content)
                    body = json.dumps(data, ensure_ascii=False).encode("utf-8")
                except Exception:
                    pass  # 解析失败则原样返回
                self._send_bytes(response.getcode(), body, content_type)
```

- [ ] **Step 4: 验证 — 确认正则过滤函数可用**

Run: `python -c "import sys; sys.path.insert(0, 'server'); from local_chat_server import _strip_brackets; assert _strip_brackets('您好（微笑）今天天气不错') == '您好今天天气不错'; assert _strip_brackets('没问题（点头）好的') == '没问题好的'; assert _strip_brackets('（开心）谢谢您') == '谢谢您'; assert _strip_brackets('没有括号的文本') == '没有括号的文本'; print('PASS')"`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add server/local_chat_server.py
git commit -m "feat: local_chat_server — 添加括号正则过滤安全网

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 8: 创建 test_composer_v13.py 冒烟测试

**Files:**
- Create: `server/prompts/test_composer_v13.py`

- [ ] **Step 1: 创建 v1.3 冒烟测试**

```python
"""
prompt_composer v1.3 与服务端 prompt 模块的冒烟测试。

用法:
    cd server/prompts
    python test_composer_v13.py
"""

from __future__ import annotations

import importlib.util
import sys
import unittest
from pathlib import Path
from typing import Any

_DIR = Path(__file__).resolve().parent
if str(_DIR) not in sys.path:
    sys.path.insert(0, str(_DIR))


def _load(filename: str) -> Any:
    path = _DIR / filename
    if not path.is_file():
        raise FileNotFoundError(path)
    mod_name = "_testv13_" + filename.replace(".py", "").replace(".", "_")
    spec = importlib.util.spec_from_file_location(mod_name, path)
    if spec is None or spec.loader is None:
        raise ImportError(f"Cannot load {path}")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module


_gp13 = _load("global_prompt_v1.3.py")
_dg13 = _load("daily_greeting_v1.3.py")
_mc13 = _load("memory_chat_v1.3.py")
_ct13 = _load("cognitive_test_v1.3.py")
_es13 = _load("emotion_support_v1.3.py")


class TestV13NoBracketPatterns(unittest.TestCase):
    """验证所有 v1.3 模块不含括号负例模式。"""

    def test_global_v13_no_bracket_examples(self):
        r = _gp13.get_prompt()
        self.assertNotIn("（微笑）", r["content"])
        self.assertNotIn("（关心地）", r["content"])
        self.assertNotIn("错误", r["content"])

    def test_global_v13_bracket_ban_still_first(self):
        r = _gp13.get_prompt()
        ban_idx = r["content"].find("不用括号表达动作")
        self.assertGreater(ban_idx, 0)
        correct_idx = r["content"].find("不纠正老人记忆")
        self.assertGreater(correct_idx, ban_idx)

    def test_daily_greeting_v13_no_bad_examples(self):
        r = _dg13.get_prompt(missing_fields=["breakfast"])
        self.assertNotIn("（微笑）", r["content"])
        self.assertNotIn("差", r["content"])
        self.assertNotIn("风格提醒", r["content"])

    def test_daily_greeting_v13_has_good_examples(self):
        r = _dg13.get_prompt(missing_fields=["breakfast", "lunch"])
        self.assertIn("话术示范", r["content"])
        self.assertIn("今儿早点", r["content"])

    def test_memory_chat_v13_no_bad_examples(self):
        r = _mc13.get_prompt()
        self.assertNotIn("（点头）", r["content"])
        self.assertNotIn("差", r["content"])
        self.assertNotIn("风格提醒", r["content"])

    def test_memory_chat_v13_has_good_examples(self):
        r = _mc13.get_prompt()
        self.assertIn("话术示范", r["content"])
        self.assertIn("冻手吧", r["content"])

    def test_cognitive_test_v13_no_bad_examples(self):
        r = _ct13.get_prompt(image_path="/test.jpg")
        self.assertNotIn("（微笑）", r["content"])
        self.assertNotIn("差", r["content"])
        self.assertNotIn("风格提醒", r["content"])

    def test_cognitive_test_v13_has_good_examples(self):
        r = _ct13.get_prompt(image_path="/test.jpg")
        self.assertIn("话术示范", r["content"])
        self.assertIn("您瞅瞅", r["content"])

    def test_emotion_support_v13_no_style_reminder(self):
        r = _es13.get_prompt()
        self.assertNotIn("风格提醒", r["content"])

    def test_emotion_support_v13_structure_intact(self):
        r = _es13.get_prompt(trigger_keywords=["想她"])
        self.assertIn("三步结构", r["content"])
        self.assertIn("共情倾听", r["content"])


class TestComposerV13(unittest.TestCase):
    """验证 composer v1.3 组装逻辑。"""

    @classmethod
    def setUpClass(cls):
        from prompt_composer import compose_system_prompt, compose_for_request
        cls.compose_system_prompt = staticmethod(compose_system_prompt)
        cls.compose_for_request = staticmethod(compose_for_request)

    def test_default_version_is_v13(self):
        r = self.compose_system_prompt()
        self.assertEqual(r["prompt_meta"]["version"], "v1.3")

    def test_no_dash_separator(self):
        r = self.compose_system_prompt(
            active_task="memory_chat",
            task_params={"conversation_context": "天冷"},
        )
        self.assertNotIn("---", r["content"])

    def test_tail_format_guard_present(self):
        r = self.compose_system_prompt()
        self.assertIn("【输出格式】不用括号表达动作或表情。", r["content"])

    def test_tail_guard_near_end(self):
        """尾部格式提醒应出现在 prompt 末尾 50 字符内。"""
        r = self.compose_system_prompt()
        content = r["content"]
        tail_pos = content.rfind("【输出格式】")
        self.assertGreater(tail_pos, 0)
        self.assertLess(len(content) - tail_pos, 50)

    def test_all_modules_loaded(self):
        """确保 v1.3 所有模块可加载。"""
        for task in ["daily_greeting", "memory_chat", "cognitive_test", "emotion_support"]:
            r = self.compose_system_prompt(
                active_task=task,
                task_params={},
                version="v1.3",
            )
            self.assertEqual(r["role"], "system")
            self.assertGreater(len(r["content"]), 50)

    def test_v12_still_works(self):
        r = self.compose_system_prompt(version="v1.2")
        self.assertEqual(r["prompt_meta"]["version"], "v1.2")
        self.assertIn("晚辈家人", r["content"])

    def test_compose_for_request_v13(self):
        ctx = {
            "global": {"dialect": "天津话", "elder_profile_brief": "王阿姨，天津人"},
            "memory_snippets": ["东北林场工作"],
            "active_task": "memory_chat",
            "task": {"conversation_context": "天冷"},
        }
        r = self.compose_for_request(ctx)
        self.assertIn("王阿姨", r["content"])
        self.assertIn("东北林场工作", r["content"])
        self.assertIn("话术示范", r["content"])
        self.assertNotIn("（点头）", r["content"])

    def test_memory_block_with_tail_guard(self):
        r = self.compose_system_prompt(
            memory_snippets=["东北林场工作，冬天很冷"],
        )
        self.assertIn("可自然提及的压缩记忆摘要", r["content"])
        self.assertIn("【输出格式】", r["content"])


class TestV13BracketBanStrength(unittest.TestCase):
    """验证括号禁令在 prompt 首尾都存在。"""

    @classmethod
    def setUpClass(cls):
        from prompt_composer import compose_system_prompt
        cls.compose = staticmethod(compose_system_prompt)

    def test_ban_at_front(self):
        """禁令应在 global block 第 1 条（prompt 前 500 字符内）。"""
        r = self.compose()
        front = r["content"][:500]
        self.assertIn("不用括号表达动作", front)

    def test_ban_at_tail(self):
        """禁令应在末尾 50 字符内。"""
        r = self.compose()
        tail = r["content"][-50:]
        self.assertIn("不用括号表达动作", tail)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行全部测试**

```bash
cd server/prompts && python test_composer_v13.py -v
```

Expected: All tests PASS

- [ ] **Step 3: 运行旧版测试确保不退化**

```bash
cd server/prompts && python test_composer_v12.py -v
```

Expected: All 31 tests still PASS

- [ ] **Step 4: 提交**

```bash
git add server/prompts/test_composer_v13.py
git commit -m "test: v1.3 冒烟测试 — 验证无括号负例+尾部格式提醒+版本隔离

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 9: 端到端验证

- [ ] **Step 1: 模拟完整 prompt 拼接并检查不包含任何括号模式**

```bash
python -c "
import sys; sys.path.insert(0, 'server/prompts')
from prompt_composer import compose_system_prompt

# 模拟真实场景: memory_chat + 记忆片段
r = compose_system_prompt(
    active_task='memory_chat',
    task_params={'conversation_context': '天冷'},
    memory_snippets=['东北林场工作']
)
content = r['content']

# 检查所有可能的括号模式
bad_patterns = ['（微笑）', '（点头）', '（关心地）', '（叹气）', '（轻声）', '（温和地）', '（停顿）']
for bp in bad_patterns:
    assert bp not in content, f'FAIL: found {bp}'

# 检查分隔符
assert '---' not in content, 'FAIL: found --- separator'

# 检查版本
assert r['prompt_meta']['version'] == 'v1.3', 'FAIL: wrong version'

print('PASS — prompt contains no bracket patterns, no --- separator, version v1.3')
print(f'Total chars: {len(content)}')
"
```

Expected: PASS

- [ ] **Step 2: 提交最终验证**

```bash
git add -A
git commit -m "chore: 端到端验证通过 — v1.3 prompt 无括号模式

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
